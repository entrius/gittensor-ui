services:
  gittensorui:
    container_name: gt-ui
    build: .
    restart: always
    ports:
      - "127.0.0.1:8080:8080"
    networks:
      - gittensor_network

networks:
  gittensor_network:
    external: true
